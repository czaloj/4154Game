package game;

import weapon.WeaponData;

class MenuLevelModifiers {
    public var characterWeapons:Array<WeaponData>; // 5 weapons for each character, and an extra that represents the flare gun
    public var enemyWeapons:Array<WeaponData>;
    
    public function new() {
        // Empty
    }
}
